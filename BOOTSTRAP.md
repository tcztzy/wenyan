# Wenyan 自舉準備（`wenyan.wy`）

目前狀態：
- 已建立自舉入口與最小詞法骨架：`wenyan.wy`。
- 已有 `分詞`（持續擴充中），可掃描完整關鍵詞、數字、`「「」」/『』` 引言與 `「」` 名。
- 已加入測試 `tests/test_bootstrap_prep.py` 驗證：
  - `wenyan.wy` 可編譯且可執行。
  - `分詞` 可對最小示例產生 token 列。

## 里程碑

1. 詞法（Stage 1，進行中）
   - [x] 最小字元掃描與 token 輸出（`分詞`）
   - [~] 對齊 `SPEC.md`：已補完整關鍵詞、最長匹配、`言/名` 區分與未盡錯誤；尚待字串跳脫、註釋/宏處理、錯誤定位

2. 文法（Stage 2，進行中）
   - [~] 已有 `析句列` 文法骨架：支援 `吾有/今有`、`名之曰`、`施`、`書之`、`夫`（含 `之/之長`）、`乃得/乃得矣/乃歸空無`、`昔之...今...`（含下標賦值）並輸出簡化節點
   - [~] 控制流子集（`若`/`恆為是`/`為是遍`/`凡`）、術定義（含 `其餘` 參組）與取/以施（含 `取其餘`）調用鏈已接入；待補更完整賦值邊界與其餘語法

3. 執行（Stage 3，進行中）
   - [~] 已有最小解譯器 `解譯`：可執行 `宣告/命名/夫/施/書之/返回/昔今` 子集（含下標賦值）
   - [~] 控制流子集（`若`/`恆為是`/`為是遍`/`凡`）、術定義（參數綁定、柯里化、`其餘` 參）與取/以施（含 `取其餘`）已可執行；待補容器與更完整數值語義

4. 自編譯（Stage 4）
   - 以 Python `wenyan.py` 編譯 `wenyan.wy`，產出可運行版本
   - 逐步削減對 Python 表達式逃逸的依賴

## 收斂準則

- `wenyan.wy` 可被現有 `wenyan.py` 編譯並運行。
- `wenyan.wy` 能解釋至少一個最小 Wenyan 程式。
- 新增語法/語義時，優先補對應測試。
